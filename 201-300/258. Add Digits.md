<?php
class Solution {

    /**
     * @param Integer $num
     * @return Integer
     */
    function addDigits($num) {
        do{
            $num = str_split($num);
            $total = array_sum($num);
            $num = $total;
        }while($total>=10);
        return $total;
    }
}
