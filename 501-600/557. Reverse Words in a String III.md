<?php
class Solution {
    function reverseWords($s) {
        $words = explode(' ', $s);
        foreach($words as &$word){
            $word = strrev($word);
        }
        return implode(' ',$words);
    }
}
