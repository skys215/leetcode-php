<?php
class Solution {
    /**
     * @param Integer $n
     * @return Integer
     */
    function reverseBits($n) {
        $rev = strrev(decbin($n));
        $rev = str_pad($rev, 32, '0', STR_PAD_RIGHT);
        return bindec($rev);
    }
}

