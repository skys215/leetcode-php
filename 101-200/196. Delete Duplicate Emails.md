# Write your MySQL query statement below
DELETE FROM person 
WHERE  person.id NOT IN (
        SELECT min_id 
        FROM   (
                SELECT Min(id) AS min_id 
                 FROM   person 
                 GROUP  BY email 
                 ORDER  BY id ASC
         )AS p
);

